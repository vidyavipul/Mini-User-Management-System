// Shared constants for the backend application
export const DB_NAME = 'mini-user-management-system-backend';

export const USER_ROLES = {
	ADMIN: 'admin',
	USER: 'user',
};

export const USER_STATUS = {
	ACTIVE: 'active',
	INACTIVE: 'inactive',
};

export const DEFAULT_PAGE_SIZE = 10;
